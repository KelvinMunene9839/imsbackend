import 'package:flutter/material.dart';

class InterestTab extends StatelessWidget {
  const InterestTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Interest Tab (interest earned, trends)'));
  }
}
